import React from 'react';

const Counter = () => {
    return (
        <div>
            <button className='btn btn-primary'>hello from counter</button>
        </div>
    );
};

export default Counter;